{
  "name": "bedrockbridge",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@8.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "pnpm install",
    "build": "pnpm -w -s run build",
    "lint": "eslint --ext .ts .",
    "test": "pnpm -w -s run test"
  }
}
