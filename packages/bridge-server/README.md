# Bridge Server package

This package contains the Bridge Server implementation for BedrockBridge.

Commands
- pnpm install (from repository root)
- pnpm --filter @bedrockbridge/bridge-server... run build
- pnpm --filter @bedrockbridge/bridge-server... run start

